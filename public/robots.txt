# Minnie — allow all crawlers
User-agent: *
Allow: /

# Sitemap (generated by @astrojs/sitemap at build time).
# Crawlers read robots.txt from the domain root, which is where it now lives —
# the site moved off the github.io project subpath on 2026-08-23.
Sitemap: https://heyminnie.com/sitemap-index.xml
